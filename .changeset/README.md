# Changesets

Changesets are configured for future release management in this repository.

The first npm publish remains `0.1.0`. This setup does not publish packages,
create tags, create release commits, or bump versions.

Use `pnpm changeset` only when a future change needs a release note.

Do not run `changeset version` or `changeset publish` without explicit approval.
