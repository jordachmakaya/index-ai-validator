---
title: Reviewer report — T1.2 Attribution module
description: Independent conformance review of rewriteAttributionSrc (tester ecaa88e, coder b88d511) — verdict PASS.
type: report
status: current
job_ref: T1.2_attribution-module
last_update: 2026-07-03
---

# T1.2_attribution-module — Reviewer report

## Verdict

**PASS**

## Scope verification

- `git show ecaa88e --stat` → touches only
  `packages/validator/src/utils/attribution.test.ts` (+87 lines, new file).
- `git show b88d511 --stat` → touches only
  `packages/validator/src/utils/attribution.ts` (+46 lines, new file).
- `git diff ecaa88e b88d511 -- packages/validator/src/utils/attribution.test.ts`
  → empty. Coder did not touch the test file. Confirmed independently.
- `git status` on `job/T1.2-attribution-module` → clean, nothing uncommitted.
- Out-of-scope files (`client/scanner-client.ts`, `utils/url.ts`,
  `utils/html-report.ts`, `cli.ts`, `types.ts`) — untouched by either commit.

## Checklist — detail

| Item | Result | Note |
|---|---|---|
| Test file untouched by coder | PASS | Diff between commits on the test file is empty. |
| `@filemeta` present/accurate on `attribution.ts` | PASS | Present, `functions`/`inputs`/`outputs`/`relations` match reality, `tested_by` points to the test file. |
| No `any` | PASS | None found. |
| `rewriteAttributionSrc` touches only `src` | PASS | Only `parsed.searchParams.set('src', src)` is called; protocol/host/pathname and every other query param are untouched. Verified by reading the function and by the test asserting `scanId`, `utm_campaign`, protocol/host/pathname, and key order all survive. |
| `src` param type restricted, derived not duplicated | PASS | `src: Exclude<AttributionSrc, 'cli-json'>` — derived from the exported union, not hand-duplicated. `pnpm check` (tsc --noEmit) exits clean, which confirms the test's `@ts-expect-error` on `'cli-json'` is necessary (not orphaned) — if the constraint were broken, tsc would report "unused @ts-expect-error" (TS2578) and `check` would fail. |
| `InvalidAuditUrlError` dev-friendly message | PASS | `Invalid audit URL: "<input>". Expected the absolute audit URL already received from the scanner in meta.links.audit, for example https://agent-view.com/audit?src=cli-json&scanId=...` — quotes the faulty value, explains, gives a concrete example. Matches `InvalidTargetUrlError` in `utils/url.ts` in structure and quality. |
| No hand-built audit URL elsewhere | PASS | Only `new URL()` + `URLSearchParams.set()` are used; no string concatenation of query params anywhere in the module. |
| No dependency outside `LIBS_REGISTRY.md` | PASS | Native `URL`/`URLSearchParams` only; registry explicitly documents "aucune nouvelle dépendance runtime" for the scan upgrade. |
| No out-of-scope file touched | PASS | Confirmed via `git show --stat` on both commits. |
| All 6 test cases green | PASS | `pnpm --filter @hardmachinelabs/index-ai-validator test -- attribution` → `src/utils/attribution.test.ts (6 tests)` all green (full suite: 134/134 passed, 13 files). Covers: rewrite to `cli-report`, rewrite to `cli-terminal`, preservation of `scanId`/protocol/host/path/extra params/key order, malformed URL → `InvalidAuditUrlError`, and the two type-level rejection cases for `'cli-json'`. |
| `pnpm check` (typecheck) | PASS | `tsc --noEmit` exits clean, no errors. |

## Skills invoked

- `shokunin-review`: one non-blocking finding — `parseAbsoluteAuditUrl` (attribution.ts) is structurally identical to `parseAbsoluteUrl` in `utils/url.ts` (try/catch around `new URL()`, throwing a custom error). Minor DRY overlap, not blocking: each throws a distinct, purpose-specific error class with a distinct message; extracting a shared generic helper would add a type-parameterized abstraction for a single duplicated 6-line pattern (arguably `yagni` in the other direction). No `any`, no reinvented URL library, no dead flexibility. `@filemeta` clean.
- `code-review`: no correctness bugs found. `searchParams.set('src', src)` correctly replaces the existing `src` value in place (preserving key order) without touching any other param or the base URL; type narrowing via `Exclude<AttributionSrc, 'cli-json'>` is sound and enforced at compile time (confirmed via `pnpm check`).

## Independent re-verification performed

- Re-ran `pnpm --filter @hardmachinelabs/index-ai-validator test -- attribution` myself (not trusting prior reports) → 134/134 green, including all 6 attribution cases.
- Re-ran `pnpm --filter @hardmachinelabs/index-ai-validator check` myself → clean, 0 errors.
- Re-ran the scope diff independently (`git show --stat` on both commits, `git diff` between them on the test file) rather than trusting the coder/tester reports.

## Conclusion

No blocking findings. One non-blocking craft observation (minor DRY overlap
with `utils/url.ts`'s parse-and-throw pattern) noted for awareness, does not
block the gate. Recommend CTO proceeds to commit and close T1.2.
