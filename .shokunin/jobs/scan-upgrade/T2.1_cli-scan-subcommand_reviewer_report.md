---
title: Report — Reviewer — T2.1 CLI scan subcommand
description: Verified ff0193a ("feat: add scan subcommand to CLI") against the full T2.1 reviewer blocking checklist — zero any/ts-expect-error/eslint-disable, clean vitest (35/35) and tsc --noEmit, no out-of-scope files touched, no new dependency, stdout/stderr contracts and error propagation all conform to spec. Verdict: VERIFIED.
type: report
status: current
job_ref: T2.1_cli-scan-subcommand
last_update: 2026-07-03
---

# Report — Reviewer — T2.1_cli-scan-subcommand

## Scope of review

Commit `ff0193a` ("feat: add scan subcommand to CLI"), the only commit
between the tester's baseline (`9807a68`) and HEAD. `git diff --stat 9807a68
ff0193a` shows exactly one file changed: `packages/validator/src/cli.ts`
(140 insertions, 1 deletion). No other file in the repo was touched by the
Coder.

## Checklist verification (each item from JOB.md, checked against the code)

1. **`cli.test.ts` not modified by Coder beyond tester's commit** — VERIFIED.
   `git diff --stat 9807a68 ff0193a` touches only `cli.ts`.

2. **Existing `validate` tests still green** — VERIFIED. `npx vitest run
   src/cli.test.ts` → `35 tests | 35 passed`, including all 24 pre-existing
   `validate` tests (no regression).

3. **No `@ts-expect-error`/`@ts-ignore`/`eslint-disable`/`any`/`as any` in
   `cli.ts`** — VERIFIED. `grep -n '\bany\b|@ts-expect-error|@ts-ignore|eslint-disable|as any' cli.ts`
   returns a single match: the substring "any" inside the pre-existing
   `--fail-on-warn` help string (`'Treat any warning as a failed validation
   result'`), not a type escape. Zero actual occurrences of the forbidden
   constructs.

4. **`scan --json` → stdout = raw `ScanStatus`** — VERIFIED. Line 104:
   `` stdout += options.json ? `${JSON.stringify(outcome.status, null, 2)}\n` : ... ``
   — exact raw `outcome.status`, no enrichment, no promo text mixed into
   stdout (the stderr message is appended to the separate `stderr`
   accumulator at line 107, never `stdout`).

5. **`scan` without `--json` → human summary, not raw JSON** — VERIFIED via
   `formatScanSummary` (lines 258–275): `URL`/`Score`/`Verdict`/`P0`/`P1`/`P2`
   lines, used at line 105.

6. **stderr message (kleur) always present after `done`, cites
   `auditLinks.terminal`, never `.html`, never hand-built** — VERIFIED.
   `formatScanStderrMessage` (lines 289–296) uses `outcome.auditLinks.terminal`
   directly inside `cyan(...)`, unconditionally appended to `stderr` at line
   107 regardless of `--json`. No reference to `auditLinks.html` in this
   function.

7. **`onProgress` writes only to stderr** — VERIFIED. Lines 96–98:
   `onProgress: (progress) => { stderr += ... }` — never touches `stdout`.

8. **`--html <path>` minimal report written directly in `cli.ts`, no
   extension of `utils/html-report.ts`; invalid path errors before `scan` is
   called** — VERIFIED. `writeScanHtmlReport`/`formatScanHtmlReport` (lines
   298–330) are local to `cli.ts`; `utils/html-report.ts` is untouched (confirmed
   by the diff --stat above). `validateHtmlPath(options.html)` (line 90) is
   called before `scanOptions` is built and before `scan(scanOptions)` (line
   101), so an invalid path throws before scan runs. Report content includes
   title, `url`, score/verdict, and a link to `outcome.auditLinks.html`.

9. **Failures (`ScanFailedError`/transport errors) propagate to `runCli`'s
   existing mechanism, no local swallowing, no HTML on failure** — VERIFIED.
   `runScan` (lines 88–112) has no `try/catch` around `await scan(scanOptions)`;
   a rejection propagates out of the Commander action into
   `program.parseAsync`, caught only by the pre-existing outer `try/catch` in
   `runCli` (lines 115–136), identical to how `validate` failures are
   handled. `writeScanHtmlReport` is only reached after a successful
   `outcome` is obtained (line 109), so it is structurally unreachable on
   failure — no file is written.

10. **`--api-key` accepted as a no-op** — VERIFIED. Declared as a Commander
    option (line 204) and as an optional field on `ScanCliOptions` (line 35),
    but never read or forwarded anywhere in `runScan` or `ScanOptions`
    construction (lines 93–99). `grep -n apiKey cli.ts` shows only the type
    declaration and the option registration.

11. **`--timeout` uses the same `parsePositiveInteger` as `validate`** —
    VERIFIED. Line 205: `.option('--timeout <ms>', ..., parsePositiveInteger,
    DEFAULT_TIMEOUT_MS)` — identical function reference to the `validate`
    command's `--timeout` option (line 181).

12. **No redeclaration of `ScanOptions`/`ScanOutcome`/`ScanFailedError`** —
    VERIFIED. `ScanOutcome` and `ScanOptions` are imported (lines 13–14) and
    used as types only; `ScanFailedError` is never referenced in `cli.ts`
    (correctly — the design requires it to propagate unhandled, not be
    caught or type-checked locally), so it is neither imported nor
    redeclared.

13. **kleur is the only new import, already in `LIBS_REGISTRY.md`** —
    VERIFIED. Diff adds exactly one new import line: `import { bold, cyan,
    green } from 'kleur/colors'`. `.shokunin/planning/LIBS_REGISTRY.md:23`
    lists `kleur ^4.1.5` with the note "réutilisée pour le message stderr de
    `scan`". `git diff --stat 9807a68 ff0193a -- packages/validator/package.json
    packages/validator/pnpm-lock.yaml` is empty — no dependency file changed,
    confirming `kleur` was already a declared dependency (`package.json:76`)
    and this is its first real usage, not a new install.

14. **No out-of-scope file touched** — VERIFIED. `scan.ts`, `types.ts`,
    `client/scanner-client.ts`, `utils/attribution.ts`, `utils/html-report.ts`,
    `utils/format.ts` are all absent from the `ff0193a` diff.

15. **The tester's 10 (11) test cases covered and green** — VERIFIED. `npx
    vitest run src/cli.test.ts` → `35 tests | 35 passed`, including all 11
    scan-subcommand tests added by the tester job (previously red per the
    tester report, now green with no modification to the test file).

## Commands run and results

```
npx vitest run src/cli.test.ts
 ✓ src/cli.test.ts (35 tests) 410ms
 Test Files  1 passed (1)
      Tests  35 passed (35)
```

```
npx tsc --noEmit -p .
(no output — clean)
```

```
git diff --stat 9807a68 ff0193a
 packages/validator/src/cli.ts | 141 +++++++++++++++++++++++++++++++++++++++++-
 1 file changed, 140 insertions(+), 1 deletion(-)
```

## Shokunin / craft observations (non-blocking)

- The HTML report writer (`formatScanHtmlReport`) is intentionally minimal
  and unstyled, matching the JOB's explicit instruction not to over-invest
  ahead of T3.1. No speculative abstraction found.
- `writeScanHtmlReport` mirrors `writeHtmlReport`'s try/catch-and-rewrap
  pattern (both produce `Failed to write HTML report: <message>`) —
  duplicated but structurally parallel to the existing `validate` path, and
  refactoring it was out of scope for this job (`utils/html-report.ts`
  extension is explicitly forbidden). Not a blocking finding; flagged only
  as a possible future DRY candidate for T3.1's CTO to consider, not an
  action for this reviewer.

## Verdict

**VERIFIED** — all blocking checklist items pass. No code was modified by
this review (reviewer role is read-only per SOUL.md).

## Handoff

Ready for the CTO to commit (already committed as `ff0193a`) and close the
T2 sprint gate: update `.shokunin/dashboard/project-dashboard.json`
(`tasks[T2.1]` → `status: done`, `evidenceStatus: verified`). T2.0 + T2.1
both VERIFIED → Sprint T2 closed. Unblocks `T5.0_resilience-hardening`.
`T3.0_html-brand-tokens`/`T3.1_scan-html-report` remain independent, not
gated by T2.1.
