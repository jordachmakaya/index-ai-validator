---
title: Report — Tester — T3.1 Rapport HTML du scan
description: Extended packages/validator/src/utils/html-report.test.ts with 4 failing TDD test cases for formatScanHtmlReport. Confirmed failing RED state.
type: report
status: current
job_ref: T3.1_scan-html-report_tester
last_update: 2026-07-04
---

# Report — Tester — T3.1_scan-html-report

## File modified

- `packages/validator/src/utils/html-report.test.ts` —
  `git diff` details the added tests and filemeta header.
  No other files were modified.

## Design followed (per JOB.md "Spécifications des Tests à écrire" — not improvised)

- **Structure Non-regression**:
  Asserts that `formatScanHtmlReport(result: ScanResult)` contains:
  - Target URL (`url` field of `ScanResult`).
  - Score (percentage, e.g., `35%`).
  - Verdict string.
  - Check/finding details (ID and title of findings present in the `ScanResult`).
- **Contextual CTA to Score**:
  Asserts that recommendations vary according to the score bracket:
  - Success (score >= 80%): positive message about AI readiness.
  - Warn (score 50%-79%): suggestion for improvement/optimization.
  - Danger (score < 50%): critical alert about AI unreadability.
- **Attribution Link**:
  Asserts that the audit link (e.g. `meta.links.audit` parameter) is rewritten to contain `src=cli-report`.
- **Plain Speech Constraints**:
  Asserts that the generated UI text (with HTML tags stripped) contains:
  - No banned promotional/marketing words (e.g., `"premier"`, `"meilleur"`, `"révolutionnaire"`, `"best"`, `"revolutionary"`, `"please"`, `"sorry"`, `"oops"`).
  - No exclamation marks (`"!"`).

## Skill references

- Followed the `test-patterns` behavior-first testing doctrine, verifying output markup features, Plain Speech compliance, and correctness of variables and attributes without mocking generator internals.
- Added `@filemeta` header to `html-report.test.ts` as per the `shokunin-filemeta` skill rules.
- Prepared this report under the `shokunin-docmeta` guidelines (including required frontmatter metadata).

## 4 new tests written

1. `preserves structure and contains target URL, score, verdict, and findings details`
2. `generates contextual CTA recommendations corresponding to branding score ranges`
3. `rewrites the audit link with src=cli-report attribution`
4. `respects Plain Speech and contains no banned words or exclamation marks in UI text`

## Test command run

```bash
pnpm --filter @hardmachinelabs/index-ai-validator test -- html-report.test
```

## Real output (4 failed/red as expected, 4 passed)

```
✓ src/utils/html-report.test.ts (8 tests | 4 failed) 26ms
  ✓ formatHtmlReport > preserves content semantic and contains score, targets, and checks details 7ms
  ✓ formatHtmlReport > contains design token CSS variables from BRANDING.md 1ms
  ✓ formatHtmlReport > includes google fonts Outfit and Inter, and configures them in styles 1ms
  ✓ formatHtmlReport > applies negative tracking to headings and title elements 1ms
  × formatScanHtmlReport > preserves structure and contains target URL, score, verdict, and findings details 9ms
    → formatScanHtmlReport is not a function
  × formatScanHtmlReport > generates contextual CTA recommendations corresponding to branding score ranges 1ms
    → formatScanHtmlReport is not a function
  × formatScanHtmlReport > rewrites the audit link with src=cli-report attribution 1ms
    → formatScanHtmlReport is not a function
  × formatScanHtmlReport > respects Plain Speech and contains no banned words or exclamation marks in UI text 1ms
    → formatScanHtmlReport is not a function
```

The tests failed correctly (red state) due to the fact that `formatScanHtmlReport` is not yet implemented/exported by `html-report.ts`, verifying the test suite sensitivity for TDD. Pre-existing tests for `formatHtmlReport` continue to pass cleanly.
This completes the Tester role.
