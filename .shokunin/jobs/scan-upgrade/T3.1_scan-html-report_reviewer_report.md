# Reviewer Report — T3.1_scan-html-report

**Date:** 2026-07-04  
**Reviewer:** Antigravity (subagent, read-only)  
**Verdict:** ✅ **PASS**

---

## Executive Summary

The implementation for T3.1_scan-html-report passes all mandatory checklist items. The test suite runs at 100% green (161/161 tests), the TypeScript compiler emits zero errors, and the Shokunin structure validator exits clean (0 errors, 0 warnings). All external data injections are properly XSS-escaped, branding tokens match DESIGN.md, and the CLI wiring correctly writes HTML reports for both validation and scan commands.

---

## Checklist Results

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Tests not altered by Coder | ✅ PASS | Test file `@filemeta` carries `job_ref: T3.1_scan-html-report_tester`; test assertions unchanged |
| 2 | TypeScript compiles 100% clean (`tsc --noEmit`) | ✅ PASS | `pnpm --filter @hardmachinelabs/index-ai-validator check` → exit 0, no output |
| 3 | Full Vitest suite passes at 100% | ✅ PASS | `161 passed (161)` — 15 test files |
| 4 | No `@ts-ignore`, `@ts-expect-error`, `eslint-disable`, `any` | ✅ PASS | grep returned 0 results in both `html-report.ts` and `cli.ts` |
| 5 | HTML report uses branding from DESIGN.md (T3.0 tokens) | ✅ PASS | All design tokens match: `--bg: #010102`, `--surface-1: #0f1011`, `--surface-2: #141516`, `--hairline: #23252a`, `--blue: #3b82f6`, `--pass: #10b981`, `--warn: #f59e0b`, `--fail: #ef4444`; Google Fonts Outfit+Inter+JetBrains Mono loaded; negative letter-spacing on all heading elements |
| 6 | CTA contextual; no banned Plain Speech words or exclamation marks | ✅ PASS | `renderScanCta()` uses plain factual language; grep for `best|premier|meilleur|révolutionnaire|revolutionary|please|sorry|oops|!` → 0 results |
| 7 | Attribution link rewritten with `src=cli-report` | ✅ PASS | `formatScanHtmlReport(result, auditUrl)` calls `rewriteAttributionSrc(auditUrl, 'cli-report')` (L1211); test at L224–234 verifies `src=cli-report` present and `src=cli-json` absent |
| 8 | All external injections escaped via `escapeHtml(...)` | ✅ PASS | 50+ call-sites confirmed; all `result.*`, `check.*`, `f.*`, `d.*` fields routed through `escapeHtml` |
| 9 | CLI wiring in `cli.ts` writes HTML report correctly | ✅ PASS | `writeHtmlReport()` (L254–262) calls `formatHtmlReport(result)`; `writeScanHtmlReport()` (L316–328) calls `formatScanHtmlReport(result, outcome.auditLinks.html)`; path validated before I/O |
| 10 | Shokunin structure: 0 errors, 0 warnings | ✅ PASS | `check-shokunin-structure: 0 error(s), 0 warning(s)` |

---

## Shokunin Review (職人レビュー)

### html-report.ts

**`html-report.ts:L711 vs L1714: dry`** — The string `'Most websites are readable by browsers. This report checks whether yours is readable by AI agents.'` appears twice. L711 wraps a static literal in `escapeHtml()` (unnecessary). L1714 embeds it raw. Extract to `const HERO_COPY_TEXT` or `renderHeroCopy()` and apply `escapeHtml()` consistently.

**`html-report.ts:L52–666 & L1226–1704: dry`** — The CSS block (~600 lines) is duplicated almost verbatim between `formatHtmlReport` and `formatScanHtmlReport`. Extract `renderSharedStyles(): string`. Net: ~600 lines removed.

**`html-report.ts:L30: type`** — `const PACKAGE_VERSION = '0.2.0'` is a magic string with no connection to `package.json`. Consider importing from `package.json` via `resolveJsonModule: true` (already in tsconfig), or add an inline comment that this must be kept in sync.

**`html-report.ts:L1030: type`** — `renderMetric(label: string, value: unknown): string` — tighten `value` from `unknown` to `string | number | boolean` — the catch-all JSON branch in `stringify()` is never reached from callers.

**Filemeta check:** `@filemeta` present and correct in `html-report.ts`, `cli.ts`, `html-report.test.ts`, and `attribution.ts`. Fields (`type`, `title`, `description`, `functions`, `inputs`, `outputs`, `relations`, `last_update`, `tested_by`) all accurate. **No filemeta issues.**

**LOGIC.md check:** The `utils/` folder now has ≥ 3 files importing each other; no `LOGIC.md` exists. **tag: `type:` (missing cluster contract)** — non-blocking for T3.1, housekeeping ticket required.

**Error-registry check:** No new error classes in `html-report.ts` or `cli.ts`. No ERRORS.md update required.

**Shokunin score:** `net: ~600 lines possible (CSS DRY) · 3 craft fixes (dry × 2, type × 1)`

---

## Code Review (Security + Correctness)

### XSS Security

All external data passes through `escapeHtml()` which escapes `&`, `<`, `>`, `"`, `'`. The `stringify()` helper handles `boolean | number | null | object`. No XSS risk identified.

### CLI File I/O

- `validateHtmlPath()` is called before the async operation — invalid paths throw `InvalidArgumentError` caught by Commander.
- Guard `if (!result)` in `writeScanHtmlReport` prevents writing an empty report.
- Both write functions propagate errors clearly.

### Attribution Rewrite

`rewriteAttributionSrc(auditUrl, 'cli-report')` called when `auditUrl` is defined (L1211). When undefined, `finalAuditUrl` is `''` and the full-audit link is omitted — correct degraded behavior.

### Plain Speech

`renderScanCta()` text for all three score bands is factual, avoids superlatifs, contains no exclamation marks. The word "critical" in the low-score branch is descriptive (not superlative) and matches the test expectation regex `/(critical|danger|unreadability|unreadable|blocked)/i`.

---

## Test Suite Evidence

```
Test Files  15 passed (15)
     Tests  161 passed (161)
  Start at  09:43:27
  Duration  3.88s
```

html-report-specific:
```
✓ formatHtmlReport > preserves content semantic and contains score, targets, and checks details
✓ formatHtmlReport > contains design token CSS variables from BRANDING.md
✓ formatHtmlReport > includes google fonts Outfit and Inter, and configures them in styles
✓ formatHtmlReport > applies negative tracking to headings and title elements
✓ formatScanHtmlReport > preserves structure and contains target URL, score, verdict, and findings details
✓ formatScanHtmlReport > generates contextual CTA recommendations corresponding to branding score ranges
✓ formatScanHtmlReport > rewrites the audit link with src=cli-report attribution
✓ formatScanHtmlReport > respects Plain Speech and contains no banned words or exclamation marks in UI text
```

---

## Typecheck Evidence

```
> tsc --noEmit
(no output — exit 0)
```

---

## Shokunin Structure Evidence

```
check-shokunin-structure: 0 error(s), 0 warning(s)
Structure conforms to PATHS_REGISTRY.md - OK
```

---

## Craft Findings Summary (Non-Blocking)

| Tag | Location | Finding |
|-----|----------|---------|
| `dry` | L711 & L1714 | Duplicate hero copy string; L711 unnecessarily wraps a static literal in `escapeHtml` |
| `dry` | L52–666 & L1226–1704 | ~600 lines of CSS duplicated between two report functions |
| `type` | L1030 | `value: unknown` on `renderMetric` — tighten to `string \| number \| boolean` |
| `type` | `utils/` folder | Missing `LOGIC.md` for the utils cluster (out-of-scope for T3.1, housekeeping) |

None of these findings are blocking for T3.1.

---

## Final Verdict

> **PASS** — All 10 checklist items satisfied. Tests 161/161 green. TypeScript 0 errors. Shokunin structure 0 errors, 0 warnings. No forbidden patterns. XSS escaping complete and consistent. Attribution correctly rewritten to `src=cli-report`. CLI wiring correct.
